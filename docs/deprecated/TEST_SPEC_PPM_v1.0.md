# 🟥 TEST SPEC — PositionPolicyManager v1.0

## **ReferenceTrendBreak (PnL-based virtual exit policy)**

**Проект:** Trading Platform (MQL5)
**Подсистема:** Strategy / Replaceable Roles
**Тестируемая роль:** `PositionPolicyManager`
**Версия TestSpec:** v1.0
**Статус:** Draft → Master-Chat approval
**Язык:** строго MQL5

---

## 0. Нормативные основания

Данный TestSpec **ОБЯЗАН** строго соответствовать:

1. **SSP v1.0**
2. **CONTRACT_LEXICON v1.x**
3. **NAMING_RULES v1.0**
4. **ROLE_CALL_CONTRACTS_MQL5 v0.1**
5. **TECH SPEC — PositionPolicyManager v1.0**

❗
TestSpec **НЕ вводит** новых требований
и **НЕ расширяет** поведение роли.

---

## 1. Цель тестирования

TestSpec предназначен **ИСКЛЮЧИТЕЛЬНО** для проверки:

* архитектурной корректности `PositionPolicyManager`
* соблюдения инвариантов владения позицией
* корректности **PnL-based virtual exit policy**
* отсутствия runtime state
* детерминизма поведения

TestSpec **НЕ проверяет**:

❌ торговую эффективность
❌ экономический смысл параметров
❌ корректность расчёта PnL
❌ поведение других ролей

---

## 2. Общие правила тестирования (ОБЯЗАТЕЛЬНЫЕ)

1. Все тесты работают **ТОЛЬКО** через:

   ```mq5
   PositionPolicyManager_Run(...)
   ```

2. В тестах **ЗАПРЕЩЕНО**:

   ❌ MT5 API
   ❌ Coordinator
   ❌ Executor
   ❌ глобальное состояние
   ❌ `static` переменные

3. Все входные данные задаются **явно**:

   * `Intent`
   * `Snapshot.position`
   * `Feedback` (пустой / фиктивный)

4. Каждый тест **изолирован** и **детерминирован**.

---

## 3. Нормативный набор тестов

---

### PPM-1 — Determinism

**Назначение:**
Проверить, что `PositionPolicyManager` является чистой функцией от входов.

**Вход:**

* одинаковый `Intent`
* одинаковый `Snapshot.position`
* одинаковый `Feedback`

**Ожидание:**

* одинаковое количество выходов
* идентичный `PolicyAdjustedIntent`

Любая зависимость от истории → **FAIL**.

---

### PPM-2 — WANT_OPEN when no position

**Вход:**

```
intent.type = WANT_OPEN
snapshot.position.has_position = false
```

**Ожидание:**

* `outputs_count = 1`
* `output.type = WANT_OPEN`

---

### PPM-3 — Block WANT_OPEN when position exists

**Вход:**

```
intent.type = WANT_OPEN
snapshot.position.has_position = true
```

**Ожидание:**

* `outputs_count = 0`

---

### PPM-4 — NO_ACTION produces no output

**Назначение:**
Зафиксировать, что `NO_ACTION` **поглощается** и не транслируется.

**Вход:**

```
intent.type = NO_ACTION
snapshot.position.has_position = any
```

**Ожидание:**

* `outputs_count = 0`

---

### PPM-5 — Block WANT_CLOSE when no position

**Вход:**

```
intent.type = WANT_CLOSE
snapshot.position.has_position = false
```

**Ожидание:**

* `outputs_count = 0`

---

### PPM-6 — Block WANT_CLOSE from Intent even with position

**Назначение:**
Зафиксировать запрет закрытия позиции напрямую через `Intent`.

**Вход:**

```
intent.type = WANT_CLOSE
snapshot.position.has_position = true
snapshot.position.floating_pnl = 0
```

**Ожидание:**

* `outputs_count = 0`

❗
Закрытие позиции возможно **ТОЛЬКО** через exit policy.

---

### PPM-7 — Virtual Stop-Loss triggers CLOSE

**Вход:**

```
intent.type = NO_ACTION
snapshot.position.has_position = true
snapshot.position.floating_pnl <= -max_loss
```

**Ожидание:**

* `outputs_count = 1`
* `output.type = WANT_CLOSE`

---

### PPM-8 — Virtual Take-Profit triggers CLOSE

**Вход:**

```
intent.type = NO_ACTION
snapshot.position.has_position = true
snapshot.position.floating_pnl >= take_profit
```

**Ожидание:**

* `outputs_count = 1`
* `output.type = WANT_CLOSE`

---

### PPM-9 — No exit when PnL inside bounds

**Вход:**

```
intent.type = NO_ACTION
snapshot.position.has_position = true
snapshot.position.floating_pnl ∈ (-max_loss, take_profit)
```

**Ожидание:**

* `outputs_count = 0`

---

### PPM-10 — Exit policy triggers CLOSE regardless of Intent

**Назначение:**
Проверить приоритет exit policy над любым входным `Intent`.

**Вход:**

```
intent.type = NO_ACTION
snapshot.position.has_position = true
snapshot.position.floating_pnl <= -max_loss
```

**Ожидание:**

* `outputs_count = 1`
* `output.type = WANT_CLOSE`

---

### PPM-11 — Single output invariant

**Назначение:**
Гарантировать, что роль **НИКОГДА** не возвращает более одного выхода.

**Метод:**

* перебор комбинаций допустимых входов

**Ограничение перебора (УТОЧНЕНИЕ):**

> Перебор ограничивается допустимыми значениями,
> описанными в настоящем TestSpec.

**Ожидание:**

```
outputs_count ∈ {0, 1}
```

---

## 4. Негативные (архитектурные) проверки

### PPM-N1 — No forbidden reads (STATIC AUDIT)

Проверяется **аудитом реализации**, а не runtime-тестами:

❌ чтение `Snapshot.market.*`
❌ чтение `Snapshot.time.*`
❌ чтение `ExecutionResult`
❌ использование MT5 API

---

### PPM-N2 — No hidden state

**Признаки нарушения:**

* разные результаты при одинаковых входах
* зависимость от порядка тестов
* изменение поведения без изменения входов

Любой признак → **FAIL**.

---

## 5. Ограничения TestSpec

Данный TestSpec:

* **НЕ фиксирует** конкретные значения `max_loss`, `take_profit`
* **НЕ требует** конфигурационного механизма
* **НЕ допускает** расширения сценариев

Любые изменения → **новая версия TestSpec**.

---

## 6. Критерий успешного прохождения

`PositionPolicyManager v1.0` считается корректным, если:

* ❏ PPM-1 … PPM-11 пройдены
* ❏ PPM-N1, PPM-N2 выполнены
* ❏ exit policy реализована без цены и тиков
* ❏ все инварианты TECH SPEC соблюдены

---

## 7. Freeze Rule

После утверждения и прохождения тестов:

> 🔒 **TEST SPEC — PositionPolicyManager v1.0 замораживается**

---

## 8. Итог

Этот TestSpec:

* блокирует архитектурные регрессии
* полностью согласуется с TECH SPEC
* пригоден для TDD
* не стимулирует усложнение

---

### 🟢 Статус Master-Chat

> **TEST SPEC — PositionPolicyManager v1.0
> ГОТОВА К ПРИНЯТИЮ**
# 🟥 TECH SPEC — VisualTester_DecisionMaker v0.1

**Проект:** Trading Platform (MQL5)
**Подсистема:** Visual Observability
**Роль:** DecisionMaker (ROLE A)
**Тип тестера:** Visual Tester (read-only)
**Версия:** v0.1
**Статус:** Draft → Master-Chat approved
**Нормативные основания:**

* SSP v1.0
* CONTRACT_LEXICON v1.0
* NAMING_RULES v1.0
* Visual Observability Model v1.0

---

## 0. Назначение

**VisualTester_DecisionMaker v0.1** предназначен для **визуализации выходного типа `Intent` роли DecisionMaker во времени**.

Тестер:

* **НЕ** участвует в торговой логике
* **НЕ** влияет на поток данных
* **НЕ** читает входы DecisionMaker
* **НЕ** знает о других ролях
* **НЕ** принимает решений

Цель тестера — **позволить наблюдать поведение DecisionMaker как чёрного ящика**.

---

## 1. Архитектурные ограничения (ОБЯЗАТЕЛЬНЫЕ)

### 1.1 Разрешено

* читать **ТОЛЬКО** тип `Intent`
* хранить историю Intent для отображения
* использовать InfoPanel **только как UI-адаптер**
* форматировать данные **только в строки**

### 1.2 Запрещено

* читать `Snapshot`
* читать `PolicyAdjustedIntent`, `Decision`, `ExecutionResult`, `Feedback`
* обращаться к торговым API MT5
* влиять на Coordinator или роли
* интерпретировать или изменять Intent
* хранить стратегическое состояние

Нарушение любого пункта = **архитектурный дефект**.

---

## 2. Входные данные

### 2.1 Тип входа

```mq5
const Intent &intent
```

Тип `Intent` — **строго по CONTRACT_LEXICON v1.0**.

---

## 3. Выход и визуализация

### 3.1 Тип визуализации

**Intent Timeline** — лента намерений во времени.

Тестер визуализирует **ПОСЛЕДНИЙ Intent на каждый бар / цикл**.

---

## 4. Использование InfoPanel

### 4.1 Общие правила

* InfoPanel используется **исключительно** как UI-слой
* VisualTester формирует строки
* InfoPanel **НЕ** знает, что визуализируется

### 4.2 Секции панели

Используются **ТОЛЬКО 2 секции из 4**.

#### Section 0 — `DECISION MAKER`

**Заголовок:**

```
DECISION MAKER
```

**Содержимое (обязательный минимум):**

Строки в формате:

```
TYPE: <intent.type>
DIR : <intent.direction>
CONF: <intent.confidence>
```

Пример:

```
TYPE: WANT_OPEN
DIR : +1
CONF: 0.37
```

---

#### Section 1 — `INTENT HISTORY`

**Заголовок:**

```
INTENT HISTORY
```

**Содержимое:**

До **N последних Intent** (N ≥ 5, фиксируется реализацией).

Каждая строка:

```
[<idx>] <type> dir=<d> conf=<c>
```

Пример:

```
[0] WANT_OPEN  dir=+1 conf=0.37
[1] WANT_HOLD  dir=0
[2] WANT_CLOSE dir=-1 conf=0.41
```

---

### 4.3 Section 2 и 3

* **НЕ используются**
* должны оставаться пустыми

---

## 5. Поведенческие инварианты (ОБЯЗАТЕЛЬНО)

VisualTester_DecisionMaker **ОБЯЗАН**:

1. Отображать **КАЖДЫЙ Intent**, даже:

   * `NO_ACTION`
   * `WANT_HOLD`
2. Не пропускать циклы
3. Не агрегировать Intent
4. Не “сглаживать” поведение
5. Отображать `confidence` **как число**, без интерпретации

---

## 6. Жизненный цикл

### 6.1 Инициализация

* VisualTester инициализирует InfoPanel
* устанавливает заголовки секций
* не отображает данных до первого Intent

### 6.2 Обновление

На каждый вызов `Update(intent)`:

1. Обновляется Section 0
2. Обновляется история в Section 1
3. Вызывается `panel.Render()`

### 6.3 Деинициализация

* VisualTester вызывает `panel.Destroy()`
* после деинициализации на графике не должно оставаться объектов

---

## 7. Проверочные вопросы (Audit Checklist)

Audit-Chat обязан задать:

1. Можно ли заменить DecisionMaker реализацию без изменения тестера?
2. Можно ли использовать этот тестер для любой стратегии?
3. Есть ли в тестере знания о рынке?
4. Есть ли в тестере знания о риске?
5. Можно ли, глядя **ТОЛЬКО** на панель, понять характер поведения DecisionMaker?

Если на любой из вопросов ответ **«нет»** → ❌ отклонение.

---

## 8. Статус версии

* v0.1 — **минимально достаточная версия**
* расширения (цвета, стрелки, графика) — **запрещены**
* любые изменения → новая версия спецификации

---

## 9. Итог

VisualTester_DecisionMaker v0.1:

* не проверяет прибыльность
* не проверяет правильность стратегии
* **проверяет архитектурную чистоту роли DecisionMaker**

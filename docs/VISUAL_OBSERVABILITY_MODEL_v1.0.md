# 👁 ПУНКТ 3 — ВИЗУАЛЬНЫЕ ТЕСТЕРЫ ПО РОЛЯМ

## Visual Observability Model v1.0

**Status:** FIXED
**Scope:** Architecture verification & debugging
**Audience:** Developer, Master-Chat, Audit-Chat
**Change policy:** versioned only

---

## 0. Главный принцип (если нарушить — всё бессмысленно)

> **Визуальный тестер показывает ТОЛЬКО ВЫХОД роли.
> Он НИКОГДА не показывает входы и внутреннюю логику.**

Если ты видишь:

* индикаторы стратегии,
* условия if,
* расчёты риска,

— это **НЕ визуальный тестер**, а отладчик.
Нам нужен **осциллограф**, а не исходник.

---

## 1. Общая схема визуализации

Каждая роль обязана иметь:

* один **визуальный канал**
* один **тип данных**, который она визуализирует
* одно **временное измерение** (бар / тик)

Визуализация:

* пассивная
* read-only
* не влияет на систему

---

# 🟥 ROLE A — Observer

## Visual Tester: **Market Snapshot Viewer**

### Что визуализируем

**`Snapshot.market` + `Snapshot.time`**

### Как выглядит в MT5

* стандартный график цены (бары)
* поверх:

  * текущий `bid / ask`
  * `spread`
  * флаг `is_new_bar`
  * `timestamp`

### Формат

* текстовая панель (InfoPanel)
* обновление на каждый snapshot

### Зачем это нужно

* проверить, что Snapshot:

  * стабилен
  * синхронизирован по времени
  * не “прыгает”
* убедиться, что **все стратегии видят ОДИН И ТОТ ЖЕ рынок**

### Контрольный вопрос

> Совпадает ли то, что видит стратегия,
> с тем, что реально на графике?

---

# 🟥 ROLE B — Decision Maker

## Visual Tester: **Intent Timeline**

### Что визуализируем

**`Intent`**

### Как выглядит

* вертикальная лента событий по времени
* каждый бар:

  * `Intent.type`
  * `direction`
  * `confidence`

Пример:

```
[12:00] WANT_OPEN  dir=+1  conf=0.72
[12:01] WANT_HOLD
[12:02] WANT_CLOSE
```

### В MT5

* стрелки / маркеры над барами
* цвет:

  * BUY → зелёный
  * SELL → красный
  * HOLD / NO_ACTION → серый
* подпись с confidence

### Зачем это нужно

* **увидеть поведение стратегии во времени**
* проверить:

  * не “дёргается” ли она
  * не спамит ли сигналами
  * реагирует ли на Feedback

### Контрольный вопрос

> Можно ли, глядя только на эту ленту,
> понять характер стратегии?

---

# 🟥 ROLE C — Position Policy Manager

## Visual Tester: **Position Policy Trace**

### Что визуализируем

**`PolicyAdjustedIntent`**

### Как выглядит

* события добавления / отказа / трансформации
* связь с исходным Intent (tag)

Пример:

```
Intent WANT_OPEN
 → Policy: SINGLE_POSITION
 → REJECT (already in position)
```

или:

```
Intent WANT_OPEN
 → Policy: GRID
 → ADD_LEVEL #2 volume=0.20
```

### В MT5

* текстовая лента (лог-панель)
* optional: линии уровней grid / scale-in

### Зачем это нужно

* **доказать**, что методика живёт НЕ в стратегии
* проверить:

  * корректность ограничений
  * независимость от рыночной логики

### Контрольный вопрос

> Можно ли заменить стратегию,
> не меняя визуализацию Policy?

Если нет — нарушение.

---

# 🟥 ROLE D — Risk / Arbiter Policy

## Visual Tester: **Decision Gate Monitor**

### Что визуализируем

**`Decision`**

### Как выглядит

* бинарные маркеры:

  * ACCEPT → ✔
  * REJECT → ✖
  * MODIFY → ~
* причина (`reason`)

Пример:

```
[12:00] ACCEPT volume=0.10
[12:03] REJECT reason=DAILY_LIMIT
```

### В MT5

* символы под графиком
* цвет:

  * зелёный / красный / жёлтый

### Зачем это нужно

* **увидеть “ворота” системы**
* понять:

  * где и почему сделки не проходят
  * не душит ли риск стратегию

### Контрольный вопрос

> Можно ли объяснить отсутствие сделок,
> глядя ТОЛЬКО на этот тестер?

---

# 🟥 ROLE E — Executor

## Visual Tester: **Execution Facts Stream**

### Что визуализируем

**`ExecutionResult`**

### Как выглядит

* фактология:

  * попытались
  * получилось
  * частично
  * отказ

Пример:

```
[12:01] EXECUTED vol=0.10 price=1.2345
[12:05] FAILED
```

### В MT5

* точки входа / выхода
* подписи без логики

### Зачем это нужно

* отделить:

  * “нам не разрешили”
  * от “мы не смогли исполнить”

### Контрольный вопрос

> Совпадает ли факт исполнения
> с тем, что ожидалось после Decision?

---

# 🟥 ROLE F — Feedback Source

## Visual Tester: **Feedback Loop Viewer**

### Что визуализируем

**`Feedback`**

### Как выглядит

* события:

  * POSITION_OPENED
  * POSITION_CLOSED
  * STOP_HIT
* `pnl`

### В MT5

* иконки на графике
* итоговая панель PnL

### Зачем это нужно

* проверить:

  * корректность обратной связи
  * реакцию стратегии на прошлое

### Контрольный вопрос

> Получает ли стратегия
> ТОЛЬКО ту информацию,
> которую имеет право знать?

---

## 2. Критические правила (обязательные)

* ❌ визуальный тестер **не принимает решений**
* ❌ не влияет на поток данных
* ❌ не читает ничего, кроме выхода роли
* ❌ не “исправляет” поведение

---

## 3. Архитектурный бонус (очень важный)

Если:

* роль невозможно визуализировать,
* или её визуализация требует знания внутренней логики,

→ **контракт роли плохой**.

Это сильнейший диагностический инструмент.

---

## 4. Итоговая матрица

| Роль            | Тип данных           | Визуальный тестер      |
| --------------- | -------------------- | ---------------------- |
| Observer        | Snapshot             | Market Snapshot Viewer |
| Decision Maker  | Intent               | Intent Timeline        |
| Position Policy | PolicyAdjustedIntent | Position Policy Trace  |
| Risk / Arbiter  | Decision             | Decision Gate Monitor  |
| Executor        | ExecutionResult      | Execution Facts Stream |
| Feedback Source | Feedback             | Feedback Loop Viewer   |

---

# 🟥 SSP — SACRED SYSTEM PACT

## Trading Platform Architecture Law **v3.0**

**Status:** NORMATIVE (LAW)
**Scope:** Entire system
**Audience:** Developer, Master-Chat, Audit-Chat
**Change policy:** ONLY by version increment
**Supersedes:** SSP v1.x

---

## 0. Назначение

Этот документ определяет **исчерпывающий архитектурный закон системы**.

Он фиксирует:

* допустимые роли,
* допустимые архитектурные типы данных,
* допустимые направления взаимодействий,
* правила владения и доступа к данным,
* **жёсткие запреты**.

Всё, что **не разрешено явно**, считается **ЗАПРЕЩЁННЫМ**.

---

## 1. Архитектурная модель

Система строится как **последовательность ролей**, взаимодействующих
**ТОЛЬКО через контрактные типы данных**.

Архитектура:

* **НЕ описывается через модули**,
* **НЕ описывается через классы**,
* **НЕ описывается через алгоритмы**.

Архитектура = **роли + данные + поток**.

---

## 2. Роли системы (FIXED SET)

В системе существуют **ТОЛЬКО следующие роли**:

1. `Observer`
2. `DecisionMaker`
3. `PositionPolicyManager`
4. `RiskArbiter`
5. `Executor`
6. `FeedbackSource`
7. `Coordinator`

Добавление новых ролей **ЗАПРЕЩЕНО**.

---

## 3. Заменяемость ролей (Multi-Strategy Model)

Заменяемыми (плагин-ролями) являются **ТОЛЬКО**:

* `DecisionMaker`
* `PositionPolicyManager`
* `RiskArbiter`

Все остальные роли:

* **НЕ заменяемы**,
* относятся к **инфраструктуре системы**.

---

## 4. Архитектурные типы данных (FIXED SET)

В системе допускаются **ТОЛЬКО** следующие архитектурные типы данных:

1. `Snapshot`
2. `Intent`
3. `PolicyAdjustedIntent`
4. `Decision`
5. `ExecutionResult`
6. `Feedback`

Использование любых иных типов данных
для **межролевого взаимодействия**
считается **архитектурным нарушением**.

---

## 5. Snapshot — закон фактов

### 5.1 Общие правила Snapshot

`Snapshot`:

* является **immutable**,
* не содержит логики,
* не содержит ссылок на среду исполнения,
* представляет **один момент времени**.

Модификация `Snapshot` после создания
**ЗАПРЕЩЕНА**.

---

### 5.2 Структура Snapshot (нормативно)

`Snapshot` содержит **ТОЛЬКО** следующие секции:

* `market`
* `position`
* `time`
* `constraints`
* `trends` ← **SSP-approved extension**

Форма и поля всех секций **строго определяются**
`CONTRACT_LEXICON v3.x`.

---

### 5.3 Snapshot.trends — Multi-Timeframe Dataset

`Snapshot.trends`:

* содержит **набор тренд-оценок по нескольким таймфреймам**,
* является **частью Snapshot**,
* подчиняется всем его инвариантам.

#### Ownership & Access Rules

* `Observer` — **SOLE PRODUCER**
* `DecisionMaker` — **MAY read**
* `PositionPolicyManager` — **MUST NOT read**
* `RiskArbiter` — **MUST NOT depend**
* `Executor` — **NO ACCESS**
* `FeedbackSource` — **NO ACCESS**

Любое нарушение этих правил —
**SSP VIOLATION**.

---

## 6. Контракты ролей

### 6.1 Observer

**Создаёт:**

* `Snapshot`

  * включая `Snapshot.trends`

**Читает:**

* ничего

**Запреты:**

* не принимает решений,
* не знает стратегию,
* не знает про исполнение.

---

### 6.2 DecisionMaker

**Читает:**

* `Snapshot` (включая `Snapshot.trends`)
* `Feedback`

**Создаёт:**

* `Intent`

**Запреты:**

* не видит `ExecutionResult`,
* не знает `Decision`,
* не управляет позицией,
* не исполняет сделки.

---

### 6.3 PositionPolicyManager

**Читает:**

* `Intent`
* `Snapshot.position`
* `Feedback`

**Допустимые исключения:**

* MAY read `Snapshot.market.bid / ask`
  — **только** для exit / lifecycle policy
* MAY read `Snapshot.position.trailing_stop_price`
  — как factual lifecycle attribute

**Создаёт:**

* `PolicyAdjustedIntent` (0..N)

**Запреты:**

* MUST NOT perform market interpretation,
* MUST NOT read `Snapshot.trends`,
* не формирует `Intent`,
* не исполняет сделки.

---

### 6.4 RiskArbiter

**Читает:**

* `PolicyAdjustedIntent`
* `Snapshot` (ограниченно)

**Создаёт:**

* `Decision`

**Запреты:**

* не знает стратегической логики,
* MUST NOT depend on `Snapshot.trends`,
* не исполняет сделки.

---

### 6.5 Executor

**Читает:**

* `Decision`

**Создаёт:**

* `ExecutionResult`

**Запреты:**

* не принимает решений,
* не интерпретирует рынок,
* не знает стратегию.

**Исключение:**

* **единственная роль с доступом к MT5 API**.

---

### 6.6 FeedbackSource

**Читает:**

* `ExecutionResult`
* состояние позиции

**Создаёт:**

* `Feedback`

**Запреты:**

* не принимает решений,
* не управляет исполнением.

---

### 6.7 Coordinator

**Функция:**

* управляет порядком вызовов ролей

**Запреты:**

* не изменяет данные,
* не принимает решений,
* не интерпретирует рынок.

---

## 7. Нормативный поток данных (MANDATORY)

```
Observer
 → Snapshot
DecisionMaker
 → Intent
PositionPolicyManager
 → PolicyAdjustedIntent(s)
RiskArbiter
 → Decision
Executor
 → ExecutionResult
FeedbackSource
 → Feedback
 ↺ (к DecisionMaker)
```

Поток **фиксирован**.
Любое отклонение — **НАРУШЕНИЕ SSP**.

---

## 8. Naming & Lexicon Compliance

Все имена типов, полей и enum-значений
**ОБЯЗАНЫ** соответствовать:

* `NAMING_RULES v1.x`
* `CONTRACT_LEXICON v3.x`

Использование имён, отсутствующих в Lexicon,
считается **архитектурным дефектом**.

---

## 9. Intent Rules

* `Intent` выражает **желание**, а не приказ,
* не содержит SL / TP,
* не содержит механики исполнения,
* не гарантирует результата.

---

## 10. Запрещённые зависимости (CRITICAL)

Запрещено:

* `DecisionMaker → Executor`
* `DecisionMaker → Decision`
* `DecisionMaker → PolicyAdjustedIntent`
* `Executor → Intent`
* `Observer → Intent`
* `PositionPolicyManager → Snapshot.trends`
* любая роль → MT5 API, **кроме Executor**

---

## 11. Визуальная наблюдаемость (MANDATORY)

Каждая роль **ОБЯЗАНА** иметь визуальный тестер,
который отображает **ТОЛЬКО её выходной тип данных**.

Визуальный тестер:

* read-only,
* не влияет на поток,
* не содержит логики.

Отсутствие визуального тестера —
**архитектурная неполнота**.

---

## 12. Правила изменения SSP

* SSP **НЕ редактируется**,
* изменения возможны **ТОЛЬКО** через новую версию,
* реализация **ВСЕГДА** подчиняется SSP, а не наоборот.

---

## 13. Статус закона

Этот документ является **единственным источником истины**
о допустимой архитектуре системы.

Любой код или ТЗ, не соответствующие SSP,
подлежат отклонению **без обсуждения**.

---

**END OF SSP v3.0**


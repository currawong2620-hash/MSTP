# 🟥 STRATEGY SPEC — ReferenceTrendBreak v1.0

## **ARCH-COMPLIANT (REVISION A)**

**Проект:** Trading Platform (MQL5)
**Статус:** Reference / Baseline
**Версия:** v1.0
**Назначение:** архитектурная валидация end-to-end пайплайна
**Торговая цель:** отсутствует (не для прибыли)
**Язык:** строго MQL5
**Тип выхода:** **PnL-based virtual exit policy**

---

## 0. Нормативные основания

Данная стратегия **СТРОГО соответствует**:

1. **SSP v1.0**
2. **CONTRACT_LEXICON v1.x**
3. **NAMING_RULES v1.0**
4. **ROLE_CALL_CONTRACTS_MQL5 v0.1**
5. **Architecture Data Flow Model v1.0**
6. **Visual Observability Model v1.0**

❗
Никакие новые роли, архитектурные типы, поля или enum **НЕ вводятся**.

---

## 1. Назначение стратегии

`ReferenceTrendBreak v1.0` предназначена **ИСКЛЮЧИТЕЛЬНО** для:

* проверки связности архитектурных ролей
* проверки детерминизма пайплайна
* проверки жизненного цикла позиции (OPEN → HOLD → CLOSE)
* проверки **PnL-based virtual exit policy**
* проверки корректности `FeedbackSource`

Стратегия **НЕ предназначена** для:

* оптимизации
* поиска edge
* рыночного анализа
* продакшн-торговли

---

## 2. Архитектурная позиция (НОРМАТИВНО)

Стратегия **НЕ является отдельным модулем**.

Она реализуется как **конфигурация поведения существующих ролей**:

```
Observer
→ DecisionMaker
→ PositionPolicyManager
→ RiskArbiter
→ Executor
→ FeedbackSource
↺ Feedback
```

❗
Введение дополнительных ролей (например SLTPEngine) **ЗАПРЕЩЕНО**.

---

## 3. Observer — требования

### Ответственность

Observer формирует **СТРОГО нормативный `Snapshot`**
как честный факт наблюдаемого состояния среды MT5.

### Используемые данные

Используются **ТОЛЬКО поля из CONTRACT_LEXICON**:

* `Snapshot.market.close`
* `Snapshot.market.symbol`
* `Snapshot.market.tf`
* `Snapshot.time.is_new_bar`
* `Snapshot.position.*`
* `Snapshot.constraints.*`

Observer **НЕ**:

* считает индикаторы
* хранит историю
* интерпретирует рынок
* добавляет производные поля

---

Ниже — **переписанный §4 DecisionMaker** для **ReferenceTrendBreak v1.1 (Directional)**.
Только логика DM, без изменений других разделов.

---

## 4. DecisionMaker — логика сигнала (Directional, v1.1)

### Назначение

DecisionMaker реализует **детерминированную направленную логику**,
используя **только факты из Snapshot**, без индикаторов и без хранения состояния.

Логика предназначена для проверки:

* генерации `WANT_OPEN` **в обоих направлениях**
* корректной маршрутизации `direction`
* симметрии пайплайна LONG / SHORT

---

### Используемые данные Snapshot

DecisionMaker **читает только**:

```
Snapshot.time.is_new_bar
Snapshot.position.has_position
Snapshot.market.last_closes[3]
```

---

### Логика формирования Intent (Нормативно)

```
IF Snapshot.time.is_new_bar
AND Snapshot.position.has_position == false
AND last_closes[0] > last_closes[1] > last_closes[2]
→ Intent.type      = WANT_OPEN
→ Intent.direction = +1
→ Intent.confidence = 1.0

ELSE IF Snapshot.time.is_new_bar
AND Snapshot.position.has_position == false
AND last_closes[0] < last_closes[1] < last_closes[2]
→ Intent.type      = WANT_OPEN
→ Intent.direction = -1
→ Intent.confidence = 1.0

ELSE
→ Intent.type = NO_ACTION
```

---

### Инварианты (ЖЁСТКИЕ)

* всегда формируется **ровно один Intent**
* DecisionMaker **не хранит состояние**
* DecisionMaker **не использует индикаторы**
* DecisionMaker **не интерпретирует рынок**
* DecisionMaker **не читает историю вне `last_closes[3]`**
* DecisionMaker **не формирует WANT_CLOSE**

---

### Примечание (Нормативное)

Данная логика:

* **не является торговой стратегией**
* **не предназначена для получения прибыли**
* используется **исключительно** для:

  * проверки directional intent
  * проверки LONG / SHORT пайплайна
  * архитектурной валидации

Смысл сигнала **вторичен**.
Первичен факт **корректного прохождения архитектуры**.

---

## 5. PositionPolicyManager — политика владения позицией

### Инварианты (НОРМАТИВНО)

* одновременно **НЕ БОЛЕЕ одной позиции**
* если `Snapshot.position.has_position == true`:

  * любые `WANT_OPEN` **НЕ транслируются дальше**

### Invariant: CLOSE requires position (КРИТИЧНО)

```
IF Snapshot.position.has_position == false
→ any WANT_CLOSE MUST NOT be propagated
→ PositionPolicyManager produces 0 outputs
```

В текстовом виде:

> Если `Snapshot.position.has_position == false`,
> любые `WANT_CLOSE`, независимо от источника,
> **НЕ транслируются дальше**
> (PositionPolicyManager возвращает 0 `PolicyAdjustedIntent`).

### Разрешённые выходы

* `PolicyAdjustedIntent` с типами:

  * `WANT_OPEN`
  * `WANT_CLOSE`

### Запрещено

❌ пирамидинг
❌ усреднение
❌ перевороты
❌ partial close

---

## 6. PnL-based exit policy

### (virtual exit, NOT price-based SL/TP)

❗
Данная политика:

* **НЕ является price-based SL/TP**
* **НЕ отслеживает касание цены**
* **НЕ осуществляет мониторинг тиков**
* **НЕ использует ценовые уровни**
* **НЕ модифицирует ордера**

Политика реализуется **ИСКЛЮЧИТЕЛЬНО внутри PositionPolicyManager**.

---

### 6.1 Virtual Stop-Loss (PnL-based)

```
IF Snapshot.position.has_position == true
AND Snapshot.position.floating_pnl <= -max_loss
→ WANT_CLOSE
```

### 6.2 Virtual Take-Profit (PnL-based)

```
IF Snapshot.position.has_position == true
AND Snapshot.position.floating_pnl >= take_profit
→ WANT_CLOSE
```

### Свойства политики

* exit-policy, а не торговый стоп
* основана только на `Snapshot.position`
* не использует рынок или индикаторы
* детерминирована при одинаковых входах

---

## 7. RiskArbiter — правила допуска

### Поведение

* принимает `PolicyAdjustedIntent`
* формирует `Decision` со статусом:

```
ACCEPT | REJECT
```

### Ограничения

* без истории
* без адаптации
* без изменения параметров
* `MODIFY` **НЕ используется**

---

## 8. Executor — правила исполнения

### Открытие позиции

* market order
* **БЕЗ SL**
* **БЕЗ TP**
* без modify
* без retry

### Закрытие позиции

* только по `Decision(action = CLOSE)`
* market close
* без логики
* без знания причин

Executor **НЕ**:

❌ сравнивает цену
❌ знает про exit-policy
❌ принимает решения

---

## 9. FeedbackSource — события

Feedback формируется **ТОЛЬКО** из `ExecutionResult[]`.

Используются **ТОЛЬКО допустимые enum**:

* `POSITION_OPENED`
* `POSITION_CLOSED`
* `ACTION_REJECTED`

❌ Запрещено:

* новые enum
* различение SL / TP на уровне Feedback
* интерпретация причин

---

## 10. Инварианты стратегии (ЖЁСТКИЕ)

Стратегия **ДОЛЖНА**:

* быть **детерминированной при одинаковых входах `Snapshot` и `Feedback`
  и одинаковом порядке вызовов ролей**
* не требовать market-independence
* работать без modify-операций
* работать без стоп-ордеров
* не расширять Lexicon
* проходить полный архитектурный пайплайн

---

## 11. Запрещённые элементы

❌ реальные SL/TP в ордере
❌ OrderModify
❌ trailing (ценовой)
❌ partial close
❌ scaling
❌ multi-position
❌ индикаторы
❌ новые типы / поля / роли

---

## 12. Статус и жизненный цикл

* стратегия **НЕ развивается**
* изменения **ЗАПРЕЩЕНЫ**
* используется как:

  * baseline
  * контрольная
  * регрессионная

---

## 13. Критерий успеха v1.0

Версия 1.0 считается **РАБОТОСПОСОБНОЙ**, если:

> появляется `WANT_OPEN`
> → позиция открывается
> → PnL-based exit policy закрывает её
> → `Feedback` корректен
> → архитектура не нарушена

---

## 14. Итог (НОРМАТИВНО)

`ReferenceTrendBreak v1.0` —
**это тест архитектуры, а не рынка**.

Если она работает:

* архитектура жизнеспособна

Если нет:

* добавление любой стратегии **ЗАПРЕЩЕНО**


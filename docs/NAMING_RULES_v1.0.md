# 📐 NAMING RULES

## System-wide Naming Conventions v1.0

**Status:** FIXED
**Scope:** All architecture contracts, data structures, modules
**Change policy:** versioned only

---

## 0. Цель правил нейминга

Цель нейминга — **не читаемость и не стиль**, а:

> **однозначное соответствие имени и смысла
> во всех модулях системы.**

Если имя допускает два толкования —
оно **запрещено**.

---

## 1. Общий принцип (главный)

### 🔒 ОДИН СМЫСЛ → ОДНО ИМЯ → ВЕЗДЕ

Если в системе есть:

* `low` — это **ВСЕГДА одно и то же**
* других вариантов **НЕ существует**

Любое отклонение:

* `low_bar`
* `barLow`
* `Low`
* `l`

считается **нарушением контракта**.

---

## 2. Язык и регистр

### 2.1. Язык

* **ТОЛЬКО английский**
* транслитерация запрещена

---

### 2.2. Регистр

| Сущность         | Формат             | Пример            |
| ---------------- | ------------------ | ----------------- |
| Типы / структуры | `PascalCase`       | `Snapshot`        |
| Роли             | `PascalCase`       | `DecisionMaker`   |
| Поля данных      | `lower_snake_case` | `entry_price`     |
| Константы / enum | `UPPER_SNAKE_CASE` | `WANT_OPEN`       |
| Методы           | `lowerCamelCase`   | `buildSnapshot()` |

❌ `camelCase` для полей запрещён
❌ `PascalCase` для полей запрещён

---

## 3. Правила ИМЁН ПОЛЕЙ (самое важное)

### 3.1. Имя поля = ЧТО ЭТО, а не ОТКУДА

❌ `low_bar`
❌ `signal_low`
❌ `market_low`

✅ `low`

Контекст задаётся **структурой**, не именем поля.

---

### 3.2. Никаких сокращений

❌ `vol`
❌ `ts`
❌ `dir`

✅ `volume`
✅ `timestamp`
✅ `direction`

Исключения:

* `id`
* `tf`
* `sl`
* `tp`

*(и только если они зафиксированы в Lexicon)*

---

### 3.3. Единственное число

❌ `prices`
❌ `bars`

✅ `price`
✅ `bar`

Массивность выражается **типом**, не именем.

---

### 3.4. Булевы поля

Только утвердительная форма:

✅ `has_position`
✅ `is_new_bar`
✅ `is_allowed`

❌ `no_position`
❌ `not_allowed`

---

## 4. Правила ИМЁН ТИПОВ (структур)

### 4.1. Тип = сущность, не действие

❌ `MarketSnapshotBuilder`
❌ `RiskChecker`

✅ `Snapshot`
✅ `Decision`

---

### 4.2. Никаких версий в имени типа

❌ `SnapshotV2`
❌ `Intent_1_3`

Версии фиксируются **документом**, а не кодом.

---

## 5. Пространство имён и вложенность

### 5.1. Контекст задаётся структурой

```text
Snapshot.market.low
```

а не:

```text
snapshot_market_low
```

---

### 5.2. Запрещено дублирование контекста

❌ `market_low` внутри `market`
❌ `position_volume` внутри `position`

---

## 6. Запрещённые паттерны (жёстко)

### ❌ Источник в имени

* `from_signal`
* `bar_low`
* `exec_price`

### ❌ Назначение в имени

* `allowed_volume`
* `risk_adjusted_lot`

(назначение — задача логики, не данных)

---

## 7. Enum и категории

### 7.1. Enum — закрытый словарь

Enum **ВСЕГДА**:

* UPPER_SNAKE_CASE
* без чисел
* без префиксов модулей

```text
WANT_OPEN
WANT_CLOSE
REJECT
ACCEPT
```

❌ `SIG_WANT_OPEN`
❌ `TO_ACCEPT`

---

## 8. Правило расширения

Если:

* появилось новое поле,
* и нет подходящего имени в Lexicon,

то:

1. код **НЕ ПИШЕТСЯ**
2. сначала обновляется Lexicon
3. только потом реализация

Это обязательное правило.

---

## 9. Контрольный вопрос (для аудита)

Audit-Chat задаёт один вопрос:

> **Можно ли, глядя только на имя поля,
> однозначно понять, что это за сущность
> без знания модуля?**

Если нет — ❌ нарушение.

---

## 10. Статус фиксации

* правила обязательны для всех модулей
* нарушение = архитектурный дефект
* изменения — только новой версией документа

---

**END OF NAMING RULES v1.0**

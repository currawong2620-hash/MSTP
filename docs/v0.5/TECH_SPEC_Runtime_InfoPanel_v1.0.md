# 🟥 SPEC — Runtime InfoPanel v1.0

**Проект:** Trading Platform (MQL5)
**Подсистема:** Runtime UI (Runner-level)
**Тип:** Operational Status Panel
**Статус:** FIXED
**Authority:** Master-Chat
**Архитектурный статус:** **NON-ARCHITECTURAL**
**Change policy:** versioned only

---

## 0. Назначение

**Runtime InfoPanel v1.0** предназначен для **операторского наблюдения**
за **фактическим состоянием работающей торговой системы** в реальном времени.

InfoPanel:

* **НЕ является** Visual Tester
* **НЕ участвует** в архитектурном потоке данных
* **НЕ влияет** на принятие решений
* **НЕ раскрывает** стратегическую логику
* **НЕ зависит** от типа стратегии (dummy / real)

Цель InfoPanel — ответить оператору на вопрос:

> **«Что система СЕЙЧАС имеет и ЧТО ОНА УЖЕ СДЕЛАЛА?»**

---

## 1. Архитектурная позиция

### 1.1 Уровень размещения

Runtime InfoPanel существует **ИСКЛЮЧИТЕЛЬНО** на уровне:

```
Runner (.mq5)
```

InfoPanel **ЗАПРЕЩЕНО** размещать:

* в Coordinator
* в ролях (Observer / Strategy / Risk / Executor / Feedback)
* в архитектурных модулях

---

### 1.2 Источники данных (РАЗРЕШЕННЫЕ)

InfoPanel **МОЖЕТ читать**:

* `Snapshot`
* `ExecutionResult` (последний)
* `Feedback`

InfoPanel **НЕ МОЖЕТ читать**:

* `Intent`
* `PolicyAdjustedIntent`
* `Decision`
* внутренние состояния ролей

---

## 2. Общие принципы (ОБЯЗАТЕЛЬНЫЕ)

1. InfoPanel отображает **ТОЛЬКО ФАКТЫ**
2. InfoPanel **НЕ объясняет причины**
3. InfoPanel **НЕ показывает будущее**
4. InfoPanel **НЕ интерпретирует данные**
5. InfoPanel **НЕ содержит логики**
6. Отсутствие данных допустимо и отображается как факт

Нарушение любого пункта = **архитектурный дефект UI-уровня**.

---

## 3. Структура панели (ФИКСИРОВАННАЯ)

Runtime InfoPanel v1.0 состоит **РОВНО из 4 секций**.

Порядок секций **НЕИЗМЕНЯЕМ**.

---

## 🟦 Section 0 — SYSTEM STATUS

### Назначение

Показать, **жива ли система и в каком она контексте**.

### Отображаемые поля

```
SYSTEM STATUS
-------------
Symbol        : <symbol>
Timeframe    : <tf>
Trading      : ENABLED | DISABLED
Last Tick    : <timestamp>
```

### Источники

* `Snapshot.market.symbol`
* `Snapshot.market.tf`
* `Snapshot.constraints.is_trading_allowed`
* `Snapshot.time.timestamp`

### Запреты

* ❌ вычисление состояний
* ❌ интерпретация причин отключения торговли

---

## 🟦 Section 1 — POSITION STATE

### Назначение

Показать **фактическое состояние позиции**.

### Отображаемые поля

```
POSITION STATE
--------------
Has position : YES | NO
Direction    : BUY | SELL | —
Volume       : <volume>
Entry price  : <price>
Floating PnL : <money>
Bars in pos  : <count>
```

### Источники

* `Snapshot.position.has_position`
* `Snapshot.position.direction`
* `Snapshot.position.volume`
* `Snapshot.position.entry_price`
* `Snapshot.position.floating_pnl`
* `Snapshot.time.bars_since_entry`

### Правила

* Если `has_position = false`:

  * Direction = `—`
  * Volume / Entry / PnL отображаются как фактические значения (обычно 0)

---

## 🟦 Section 2 — LAST ACTION (FACT)

### Назначение

Показать **последний завершённый факт действия системы**.

### Отображаемые поля

```
LAST ACTION
-----------
Event        : <feedback_event>
PnL          : <money>
Message      : <string>
```

### Источники

* `Feedback.event`
* `Feedback.pnl`
* `Feedback.message`

### Правила

* `Message` — **чистый текст**, без логики
* Отсутствие действия допустимо (`event` может быть пустым)

---

## 🟦 Section 3 — EXECUTION HEALTH

### Назначение

Показать **факт последней попытки исполнения**.

### Отображаемые поля

```
EXECUTION HEALTH
----------------
Last result  : <execution_status>
Filled vol   : <volume>
Exec price  : <price>
```

### Источники

* последний `ExecutionResult`

### Правила

* Отображается **последний полученный результат**
* Если исполнение не вызывалось — статус отсутствует (факт)

---

## 4. Категорические запреты (ЖЁСТКО)

Runtime InfoPanel **ЗАПРЕЩЕНО**:

* показывать `Intent`
* показывать `Decision`
* показывать `PolicyAdjustedIntent`
* показывать причины решений
* показывать индикаторы
* показывать расчёты риска
* использовать цветовую кодировку как логику
* влиять на поведение системы

---

## 5. Strategy Independence Invariant

Runtime InfoPanel v1.0 **НЕ МЕНЯЕТСЯ** при замене:

* Dummy Strategy → Real Strategy
* одной стратегии → другой
* одного RiskArbiter → другого

Если InfoPanel требует изменений из-за стратегии —
**это дефект архитектуры UI**.

---

## 6. Lifecycle

### Инициализация

* создаётся в Runner
* инициализирует UI
* не отображает данные до первого Snapshot

### Обновление

* обновляется **после каждого orchestration цикла**
* использует **последние доступные факты**

### Деинициализация

* уничтожает все UI-объекты
* не оставляет следов на графике

---

## 7. Контрольный вопрос (ОБЯЗАТЕЛЬНЫЙ)

> Можно ли, глядя ТОЛЬКО на Runtime InfoPanel,
> понять:
>
> * живёт ли система,
> * есть ли позиция,
> * что она уже сделала,
> * были ли проблемы с исполнением —
>
> **НЕ зная стратегии?**

Если **да** — SPEC соблюдён.
Если **нет** — InfoPanel нарушен.

---

## 8. Статус версии

* Runtime InfoPanel **v1.0 — минимально достаточная**
* расширения → **ТОЛЬКО новой версией**
* изменения формата → **breaking change**

---

**END OF SPEC — Runtime InfoPanel v1.0**
